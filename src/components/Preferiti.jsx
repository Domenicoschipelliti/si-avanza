import { ListGroup } from "react-bootstrap";

const Preferiti = ({ jobData }) => {
  return (
    <ListGroup>
      <ListGroup.Item>messo nei preferiti u.u</ListGroup.Item>
    </ListGroup>
  );
};

export default Preferiti;
