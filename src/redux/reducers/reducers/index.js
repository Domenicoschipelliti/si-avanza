const primoState = {
  aziende: {
    job: [],
  },
};

const primoReducers = (state = primoState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default primoReducers;
